# Auto-Generated Fix for INC-EA65560C

**Severity:** HIGH
**Type:** NULL_USER_ID
**Target:** `OrderService`

## Root Cause
MDC context propagation failure in async or retry path caused user_id to be null when order was submitted, leading to downstream association failure and pipeline abort at reservation stage.

## Explanation
The logs show an order submitted without user context (NULL_USER_ID), followed immediately by order pipeline abortion at the reservation stage. This pattern is consistent with the ASYNC_ORPHAN pre-classifier hint, indicating that when OrderService processed this request (likely in an async executor or retry mechanism), the ThreadLocal MDC context containing user_id was not propagated. Without user association, the order pipeline could not proceed to reserve inventory.

## Suggested Code Change
```java
--- a/src/main/java/com/ecommerce/order/OrderService.java
+++ b/src/main/java/com/ecommerce/order/OrderService.java
@@ -15,6 +15,7 @@
 import org.springframework.retry.annotation.Retryable;
 import org.springframework.scheduling.annotation.Async;
+import org.slf4j.MDC;
 
 @Service
 public class OrderService {
@@ -45,8 +46,14 @@
     @Async
     @Retryable(maxAttempts = 3)
     public CompletableFuture<Order> submitOrder(OrderRequest request) {
+        Map<String, String> contextMap = MDC.getCopyOfContextMap();
         return CompletableFuture.supplyAsync(() -> {
+            if (contextMap != null) {
+                MDC.setContextMap(contextMap);
+            }
             try {
+                String userId = MDC.get("user_id");
+                if (userId == null) {
+                    log.warn("Order submitted without user context — downstream association unavailable");
+                    throw new IllegalStateException("Missing user_id in context");
+                }
                 return processOrderPipeline(request);
+            } finally {
+                MDC.clear();
             }
         });
     }
```

---
*Generated by AI Self-Healing Incident Assistant*
